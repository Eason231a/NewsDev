请帮我生成一个后台管理页面的前端 UI 界面代码（请指定技术栈，例如：React + Tailwind CSS / Vue 3 + Element Plus / 原生 HTML+CSS）。

### 核心要求：
*   **只需 UI 样式与结构设计**，不需要填充具体的表格业务数据（数据部分使用 2-3 行结构清晰的 Mock 占位数据或组件占位即可）。
*   要求 UI 视觉高保真：现代极简风格、合理的内外边距（Padding/Margin）、柔和的阴影、圆角卡片布局。

---

### 1. 全局与布局结构 (Layout)
*   **背景色:** 浅灰/冷灰背景（例如 `#F4F6F8`）。
*   **主容器:** 居中的圆角白色卡片容器，带有轻微阴影（`box-shadow`）。
*   **整体架构:** 包含顶部导航栏 (Header) + 左右双栏布局 (左侧 Sidebar，右侧 Main Content)。

### 2. 顶部导航栏 (Header UI)
*   **左侧:** Logo 占位区域（图标 + 平台名称文本）。
*   **中间:** 水平菜单栏组件（支持 Hover 高亮和 `Active` 选中态下划线）。
*   **右侧:** 用户个人信息区（包含圆形头像占位、用户名、操作指南按钮、退出登录按钮）。

### 3. 左侧侧边栏 (Sidebar UI)
*   **顶部:** 返回按钮与当前模块标题。
*   **菜单组件:** 垂直手风琴/树状菜单组件。
    *   支持展开/折叠状态。
    *   选中项（Active State）需要呈现高亮视觉效果（如：左侧蓝色 Accent 竖条 + 淡蓝色背景背景块）。

### 4. 右侧主内容区 UI (Main Content Area)
*   **搜索与筛选区域 (Search Filter Panel):**
    *   包裹在一个独立的白色卡片中。
    *   响应式栅格布局（Grid / Flex），包含：3个下拉选择框（Select）、1个日期范围选择框（Date Range Picker）。
    *   右侧对齐的按钮组：主按钮（Solid Blue）和次级按钮（Outline/Text）。
*   **数据操作栏 (Action Bar):**
    *   包含标准功能按钮（如：“导出”按钮）。
*   **表格容器 UI (Table Container):**
    *   包含完整的 Table Header 样式（浅灰背景、加粗文本、清晰的底边框）。
    *   只需渲染 2-3 行示例 Mock 数据展示布局，列包括：标准文本列、日期列、以及高亮的点击数值列（蓝色文本）。
    *   **分页器组件 (Pagination UI):** 右下角对齐的分页控件，包含每页条数下拉框、页码按钮、跳转输入框。

### 5. 弹窗/抽屉组件 UI (Modal / Dialog UI)
*   预设一个浮动 Modal 弹窗组件（可通过状态控制显隐）。
*   **设计样式:** 圆角白底卡片、深色半透明 Mask 背景遮罩、顶部带关闭图标 (X)。
*   **内部结构:** 包含列表式手风琴/折叠面板 UI（带有圆形头像占位、标题、标签与小尺寸分页器）。

请输出结构清晰、样式与逻辑分离的高质 UI 代码。